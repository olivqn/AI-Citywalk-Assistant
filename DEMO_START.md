# 使用 Demo

1. 双击项目中的 `启动 Demo.cmd`，等待浏览器自动打开。
2. 在底部输入需求，点击发送或按回车。例如：从上海图书馆出发，两小时看老洋房，最后喝咖啡。
3. 后续直接打开 http://127.0.0.1:8000/；如果连接失败，再双击启动文件。

顶部显示“AI 已连接”表示本次收到真实模型回复。“基础演示”表示 AI 暂不可用，当前使用规则回复。等待时点击发送按钮位置的方块可以停止，失败或超时后可重发。

首次启动需要 Python 3.11 或更新版本，并联网安装 `requirements.txt` 中的依赖。对话和地点筛选均使用 DeepSeek 官方 `deepseek-v4-flash`，关闭深度思考以保持交互速度。真实 AI 使用本地 `.env` 中的 `DEEPSEEK_API_KEY`，请勿提交密钥。

## 开发验证

前端离线回归：`node --test tests/frontend-send.test.cjs tests/route-core.test.cjs`

后端离线回归（使用已安装项目依赖的 Python）：`python -B -m unittest discover -s tests -p test_backend_runtime.py -v`

运行日志位于 `.demo-logs/`，不会进入 Git。

## 这版 Demo 可以怎么试

- “从衡山路出发，轻松逛花园和书店，1小时。”
- “只保留美罗城和港汇恒隆广场。”
- “把路线顺序改成：港汇恒隆广场 → 美罗城。”
- “取消固定终点，其他地点保持不变。”
- “撤销上一步”，或点击路线面板的同名按钮。

地图、路线列表和行程标签只在校验通过后更新。无法满足距离上限、必去、排除或类别条件时，说明原因并保留上一条路线。距离取地图步行规划结果；含停留时间为估算，不把目标时长当实际耗时。

撤销保留本页最近 10 次成功修改；“恢复第一次路线”恢复本页首次成功路线。刷新页面会清空会话和撤销记录。这不是持久化行程管理。

地点检索使用徐汇周边的保守 Demo 覆盖范围，不是精确行政边界；有歧义时请在左上角搜索并选择带地址的候选。场馆开放时间、门票及临时封闭情况未接入实时核实。
