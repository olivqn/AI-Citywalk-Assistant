# Demo 优化与回归记录

针对 [原用户评测](C:/PythonProjects/codex/dachuangX/USER_SIMULATION_2026-09-06.md) 修改，继续使用 DeepSeek V4 Flash。所有改动留在本地，没有推送 GitHub。

## 这版解决了什么

- 聊天不再提前宣告“改好了”。先查地点、计算步行路线、检查条件，成功后再同时更新地图、列表和标签。
- 失败、超时或用户停止时保留原路线和条件，不出现“标签换起点、地图仍用旧起点”。
- 支持精确保留多个地点、指定顺序、取消固定终点、撤销上一步与恢复第一次路线。
- 地点按上海市检索，并校验保守 Demo 覆盖范围；区分衡山路地铁站和远处近似地名。地址歧义时请用户选择候选。
- 拒绝纯门牌、停车设施等非游览结果；过滤民宿/复式出租公寓类结果。
- 检查步行距离上限、目标时间、必去、排除和明确类别；书店不能靠搜索关键词或咖啡店冒充。
- 用户的普通主题不会永久升级成硬条件；取消终点不删地点，后续加站仍能保留真正终点。
- 距离来自地图步行规划；含停留时间是单独估算。地图失败时不使用直线距离冒充步行距离。
- 地点讲解降级时明确资料不足、无法核实开放情况，不让用户反复追问同一个未回答的问题。

## 真实网页回归观察

逐次通过网页发送需求，读取聊天、行程标签、路线列表和距离摘要。以下是本次具体运行的观察，不是统计成功率，也不保证同一模型每次推荐相同店铺。

| 场景 | 观察结果 |
| --- | --- |
| 衡山路出发，1 小时逛花园和书店 | 盛家花园 → 上海党建书店（高安路店），步行 644 米，含停留预计 30 分钟；不再落到横山村门牌。 |
| 上海图书馆出发，20 分钟且不超过 1 公里 | 生成的路线受真实步行距离限制；开发回归中取得 562 米/14 分钟的一站路线。 |
| 再要求压缩到 500 米以内 | 无法满足时明确显示“没有生效”，保留旧路线及旧标签；不声称满足 500 米。 |
| 指定两家咖啡店反向排列 | 地图顺序与指定顺序一致；撤销后恢复原顺序和原距离，而不是重新随机生成。 |
| 设置盛家花园为终点，再取消固定终点 | 设置后花园成为最后一站；取消后终点标签清除，两个地点和顺序保留。 |
| 上海图书馆出发，最后必须到武康大楼 | 实际路线以武康大楼结束，步行 4168 米，含停留预计 110 分钟，没有只更新终点标签。 |
| 改从徐家汇地铁站出发，保留武康大楼终点 | 顶部和路线列表起点均变为徐家汇地铁站；实际重新生成 3224 米路线，最后仍为武康大楼。 |
| 徐家汇出发，只去美罗城和港汇恒隆广场 | 只生成这两个指定地点，没有体育场或天桥，也不额外加附近站点。 |
| 在已有路线中只保留港汇和美罗城，同时排除体育场和天桥 | 实际为港汇恒隆广场 → 美罗城，916 米；列表和聊天一致。 |
| 换起点后要求恢复第一次路线 | 恢复为上海图书馆起点、原来的站点顺序和原 4168 米距离，不是再生成一条。 |

复测同时发现并修复：普通主题被当成永久必含类别、保留终点时丢失终点标记、指定地名被附近搜索替代、起点被重复加入景点、取消终点仍残留“最后一站”说明。

## 自动验证

- 43 项 JavaScript 回归：消息收发、取消/超时、完整页面执行器、地图桩、约束、调序、终点、起点、撤销、精确地点和类别筛选。
- 20 项 Python 回归：模型协议、确定性指令覆盖、清空状态、失败降级、服务与静态文件。
- 两组均通过；JavaScript 语法检查与 Git 差异空白检查通过。

命令见 [使用说明](C:/PythonProjects/codex/dachuangX/DEMO_START.md)。路线度量接入依据 [百度地图步行规划文档](https://lbsyun.baidu.com/docs/jsapi?title=jspopularGL%2Fguide%2Frouteplan)。

## Demo 的边界

这次主要解决“理解了却没执行”和“执行结果不可信”。没有接入场馆实时开放、门票或临时封闭信息；地图 POI 的准确性、模型回复稳定性仍依赖外部服务。步行加停留时长为估算，不是承诺。

覆盖范围是徐汇周边的保守矩形，不等于精确行政边界。撤销仅保存当前页最近 10 次成功修改；刷新后对话、路线和撤销记录清空。没有新增账号、数据库或复杂后台。
